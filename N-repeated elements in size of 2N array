class Solution {
    public int repeatedNTimes(int[] A) {
     int n = A.length;
        for (int i = 0; i < n; ++i)
            if (A[i] == A[(i + 1) % n] ||  A[i] == A[(i + 2) % n])
                return A[i];
  
         return A[0];
      
      }
      }
    
